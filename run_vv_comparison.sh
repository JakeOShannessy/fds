#!/bin/bash

# pushd fds-local || exit 1
# find . -name "*.csv" | sort > ../local_csvs.txt
# popd || exit 1

# pushd fds-nist || exit 1
# find . -name "*.csv" | sort > ../nist_csvs.txt
# popd || exit 1

# diff local_csvs.txt nist_csvs.txt

# while read -r p; do
#   echo "$p"
#   diff fds-local/"$p" fds-nist/"$p"
# done <local_csvs.txt



# sudo dnf install xorg-x11-server-Xvfb
